# GitHub Pages — V9
Upload `index.html` to the repository root. In Settings -> Pages choose Deploy from a branch, `main`, `/(root)`.

This package is static: the dashboard and saved ML snapshot work, but Python rescoring and email alerts require the full FastAPI package.
